---
plugin: aws_ec2
#aws_cli wich is already configured (aws configure) with IAM so we don't have our credentials in clear :)
#Check your own aws credential file to get profil name (here default)
aws_profil: default 
regions:
  - eu-west-3
ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
